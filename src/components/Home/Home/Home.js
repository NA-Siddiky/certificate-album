import React from 'react';
import Footer from '../../Footer/Footer';
import Header from '../../Header/Header/Header';

const Home = () => {
    return (
        <main id="home">
            <Header></Header>
            <Footer></Footer>
        </main>
    );
};

export default Home;